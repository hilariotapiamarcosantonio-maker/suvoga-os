SPREADSHEET_ID=
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000
CRM_BASIC_AUTH_USER=
CRM_BASIC_AUTH_PASSWORD=

# WhatsApp "concierge digital" floating button (src/components/suvoga/SuvogaWhatsAppButton.tsx).
# Leave empty until a real number exists - the button stays hidden in production
# rather than ever opening a fabricated wa.me link. Digits only or with punctuation,
# include country code, e.g. 18095551234 for Dominican Republic.
NEXT_PUBLIC_SUVOGA_WHATSAPP=
# Optional: overrides the default prefilled message for the generic (non-course) button.
NEXT_PUBLIC_SUVOGA_WHATSAPP_MESSAGE=
